Build a structured article from the compact evidence bundle. Return JSON only.
Source language: {source_language}.
Target output language tags: {target_languages}.
For every multilingual text field, return an object keyed by exactly the configured target language tags. Do not hard-code fa/en unless they are configured.
