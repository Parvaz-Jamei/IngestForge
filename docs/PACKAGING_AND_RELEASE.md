# Packaging and Release

Use PyPI Trusted Publishing through GitHub Actions. Name normalization treats runs of `.`, `_`, and `-` as equivalent separators, so publish as the single-token name `ingestforge`.
